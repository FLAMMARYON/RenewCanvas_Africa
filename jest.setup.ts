// Adds custom RTL matchers like `toBeInTheDocument`, `toHaveAttribute`, etc.
// to Jest's `expect`. Loaded once before every test file via
// `setupFilesAfterEnv` in jest.config.js.
import "@testing-library/jest-dom";
